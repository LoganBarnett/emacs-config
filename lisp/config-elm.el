;;; config-elm --- configure elm support
;;; Commentary:
;; the layer sets up a lot of this

;;; Code:

;; configure elm
(defun config-elm ()
  ;; (use-package "elm"
    ;; :init
    ;; :config
    (setq-default elm-indent-offset 2)
    ;; )
  )
(provide 'config-elm)

;;; config-elm.el ends here
