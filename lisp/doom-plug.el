;; A dumping ground for various variables and such that Doom needed but we
;; really don't.  This is more to just get things going without being surgical.

(defvar doom-disabled-packages ()
  "A list of packages that should be ignored by `use-package!' and `after!'.")

(defmacro modulep! (&rest _)
  nil
  )
